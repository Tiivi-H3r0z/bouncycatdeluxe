#pragma strict

function Start () {

}

function Update () {
transform.rotation.x = Camera.main.transform.rotation.x;
transform.rotation.y = Camera.main.transform.rotation.y;
transform.rotation.z = Camera.main.transform.rotation.z;
}
